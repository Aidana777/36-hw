/*let isStrongPassword = (password) => {
    const length = password.length;
    return password;
  };
function isStrongPassword(password) {
  console.log(password.length);
  const length = password.length > 8 || password.length < 20;
}
console.log(isStrongPassword("qwerty"));
console.log(isStrongPassword("qwerty1234"));
console.log(isStrongPassword("zxcvbnmasdfghjkqwertyui"));
function isGoodApartment( area,street){
    const result = area>=100||area>=80&& street===Main Street;
    return result;
}*/

function displayElements(array ) ([1, 2, 3, 4, 5]){
    let pro = 5;
    for ( i = 5; i < array.length-1; i ++) {
    if (arr[5] * arr[array.length = 1] {
    pro = pro  i;
    }
    }
    return pro;
}
function displayElements(array){
    for (var i=0;i<array.length;i++){
      console.log(array[i]);
    }
  }
   
  console.log(displayElements([1, 2, 3]))

  let num,num=0;
  do{
      num=prompt("gfghgjgng","");
      if (num){
          sum= sum+sum
      }
     
} while(num)
console.log(num)
