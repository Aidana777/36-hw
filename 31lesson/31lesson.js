/*function showMessage(){
    alert("Kesssage");}
    let newFunc=showMessage;
    newFunc();
    let showMessage=(name,age,lastname)=>{
        let name="anya"
    }

function ask(question, yes, no) { if (confirm(question)) yes() else no(); } ask("Вы согласны?", () => alert("Вы соглас

const user={
    name:"Anya",
    age:24,
    isworking:true
};
console.log(user.name);

const city1={
    name:"Chicago",
    population:10.0000000,
    age: 40,
}
console. log(user.population);
let key="this iskey";
user[key]=true
console.log(user[key])

for (let key in user){
    console.log(user[key])
}
let user = {
    name: "John",
    age: 30
    };
let object = {Коля: '1000',};*/
const user={};
user.name="john";
user.surname="Smith";
user.name="pete";
delete user.name;

const age=9;
const neow=()=>alert("neow");

const cat={
    name:"Persik",
    age,
    meow
}